2번째 줄에서 stdlib.h 헤더파일을 사용해 malloc, qsort, free와 같은 함수를 사용할 수 있게 하였고, 4번째 줄에서 qsort함수에서 오름차순 정렬을 위해 사용하는 compare 함수를 선언하였습니다.
그리고 14 ~ 25번 줄에서 num개의 int 타입 메모리 공간에 각 10개의 int 타입 메모리를 할당하고, 각 메모리 공간에 값을 입력받고 이 값을 qsort함수를 통해 오름차순으로 정렬하였습니다.
27번째 줄에서 값을 출력할 때, printf("%d\n", arr[i][7]); 이라고 코드를 작성한 이유는 qsort를 통해 오름차순 정렬을 했을 때, 문제에서 요구하는 3번째로 큰 값은 배열의 7번째에 위치해 있기 때문입니다.
마지막으로 29~33번 줄에서 free 함수를 통해 메모리를 해제하여 문제를 마무리했습니다.
